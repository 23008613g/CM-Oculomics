# Training / inference configuration
manifest: data/manifest.csv     # columns: image_path,label[,patient_id] -- YOUR de-identified data
retfound_weights: weights/RETFound_cfp.pth   # official RETFound weights (CC BY-NC 4.0)
out_weights: weights/best.pth
seed: 42
folds: 5
image_size: 224
batch_size: 16
lr: 0.00005
weight_decay: 0.05
epochs: 30
patience: 10
